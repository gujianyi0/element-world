# 元素世界 (Element World)

3D打印元素拟人化模型展示网站。

## 技术栈

React 18 + Vite + React Three Fiber (Three.js) + React Router + i18next

## 开发命令

```bash
npm install        # 安装依赖
npm run dev        # 启动开发服务器
npm run build      # 构建静态文件
npm run preview    # 预览构建结果
```
